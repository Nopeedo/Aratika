/** Lucide icons referenced by name in the recommendation engine. */
import {
  Scale, MapPin, GraduationCap, FileText, PenLine, Compass, Vote, BookOpen,
  Users, Eye, BarChart3, Swords, Crown, Map as MapIcon,
} from 'lucide-react'

export const REC_ICONS: Record<string, React.ElementType> = {
  Scale, MapPin, GraduationCap, FileText, PenLine, Compass, Vote, BookOpen,
  Users, Eye, Map: MapIcon, BarChart3, Swords, Crown,
}
